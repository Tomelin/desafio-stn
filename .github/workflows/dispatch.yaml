name: second-workflow

on: 
  workflow_call:
    inputs:
      kube-config:
        required: true
        type: string

jobs:
  process-output:
    runs-on: ubuntu-latest
    steps:
    - name: Access Specific Output (Example)
      run: |
        echo "Value of VAR1: ${{ github.event.inputs.kube-config }}"
